// Mirrors sync-room-node's config.maxParticipants default — purely for
// display (e.g. "8 / 10 in room"). The server is the actual source of truth
// and enforcement; update both if the cap changes.
export const MAX_PARTICIPANTS = 10;
